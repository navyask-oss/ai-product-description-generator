services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: product_ai
    ports:
      - "5432:5432"
    volumes:
      - product_ai_pg:/var/lib/postgresql/data

volumes:
  product_ai_pg:
